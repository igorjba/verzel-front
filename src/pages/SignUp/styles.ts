import styled from 'styled-components'

export const SignUpContainer = styled.main`
  width: 100%;
  height: 100%;
  max-width: 70rem;
  margin: 2rem 2.5rem;

  position: relative;

  .PaginationContainer {
    width: fit-content;
    margin: auto;

    position: absolute;
    left: 0;
    right: 0;
    bottom: 2.5rem;
  }
`